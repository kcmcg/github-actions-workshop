name: PR Verify

on:
  pull_request:
    branches: ["main"]
  workflow_dispatch:

jobs:
  build:
    name: PR Verify
    uses: kcmcg/reusable-workflows/.github/workflows/reusable-workflows.yml
    with:
      dotnet_version: 9.0.x
